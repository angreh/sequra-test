const assert = require( 'assert' ).strict

describe("structure in folder", () => {
    it("just testing", () => {
        assert.strictEqual(12, 12)
    })
    it("just testing 2", () => {
        assert.strictEqual(12, 12)
    })
})